<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.oscar.actividad3aad">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Preferencias_Activity" />
        <activity android:name=".insertarEstudiantes" />
        <activity android:name=".insertProfesores" />
        <activity android:name=".Consultas" />
        <activity android:name=".ActualizarDatos" />
        <activity android:name=".BorrarDatos" />
        <activity android:name=".InsertarAsignatura"></activity>
    </application>

</manifest>